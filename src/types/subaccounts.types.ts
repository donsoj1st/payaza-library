/*
 * this type defined this parameter
 * to create sub_account for user with already
 * existing account
 * ****/
export type subAccounts = {
  mainAccountPayazaReference: string;
  name: string;
  currency: string;
};
